### Authentication details
tenancy_ocid = ""

user_ocid = ""

fingerprint = ""

private_key_path = "~/.oci/oci_api_key.pem"

### Region
region = "us-phoenix-1"

### Compartment
compartment_ocid = ""

### Public/private keys used on the instances
ssh_public_key_path = "~/.ssh/id_rsa.pub"

ssh_private_key_path = "~/.ssh/id_rsa"

###  Name of the deployment, short way. (e.g. mydeployment).
deployment_short_name = "experiment"

### Size of each instance. (Minimum recommended: VM.Standard1.1).
instance_shape = "VM.Standard2.8"

### Number of instances to deploy. (Minimum recommended: 3).
nodes_count = "3"

###  Size of the data volume
volume_size = "200"
